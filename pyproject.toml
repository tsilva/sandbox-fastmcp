[project]
name = "sandbox-fastmcp"
version = "0.1.0"
description = "A sandbox project using fastmcp"
requires-python = ">=3.11"
dependencies = [
    "fastmcp",
    "matplotlib",
    "numpy",
    "pandas",
    "scikit-learn",
    "wandb"
]
